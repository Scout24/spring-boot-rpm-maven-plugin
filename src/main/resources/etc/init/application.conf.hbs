# Start the application as Upstart service job

description "{{description}}"

start on stopped rc
stop on runlevel [!2345]

respawn

exec su -l -s /bin/sh -c "exec /usr/share/{{application}}/start.sh" {{application}}
